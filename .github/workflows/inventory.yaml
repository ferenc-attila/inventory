name: Inventory CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1

      - uses: m4nu56/postgresql-action@1.0
        with:
          postgresql version: '14'
          postgresql db: 'postgres'
          postgresql user: 'inventory'
          postgresql password: 'inventory'
          postgresql init scripts: './'
      - uses: actions/checkout@v3
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
      - name: Build with Maven
        run: mvn verify
      - uses: actions/upload-artifact@v3
        with:
          name: inventory-demo
          path: target/*.jar

#  postgres-job:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Start PostgreSQL on Ubuntu
#        run: |
#          sudo systemctl start postgresql.service
#          pg_isready
#      - name: Create user
#        run: |
#          sudo -u postgres psql --command="CREATE USER inventory WITH PASSWORD 'inventory' VALID UNTIL 'infinity'"
#      - name: Create database
#        run: |
#          sudo -u postgres psql --command="CREATE DATABASE inventory WITH OWNER = inventory TEMPLATE = 'template0' ENCODING = 'UTF-8'"